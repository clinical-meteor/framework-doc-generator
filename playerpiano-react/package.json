{
  "name": "test",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "integration-tests": "meteor test --full-app --once --driver-package practicalmeteor:mocha",
    "cli-integration-tests": "meteor test --full-app --driver-package dispatch:mocha-phantomjs"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "meteor-node-stubs": "~0.2.3"
  }
}
